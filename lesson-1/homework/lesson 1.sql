--Create class
1) DB NAME READBOOKSDB
TABLE NAME BOOKS
COLUMNS:
 BOOKID, int
NAME, varchar
AUTHOR, 
GENRE, 
STATUS char
--Create database READBOOKSDB
go
use READBOOKSDB
create table books (bookid int,name varchar (30),author varchar (30),
,genry varchar (30),status varchar (30)
select * from books 

insert into books values (1,'Shinnelli yillar'



select * from [HumanResources].[Employee]
where Gender='M'

CREATE TABLE classes (
    class_id INT,
    class_name VARCHAR(50),
    teacher_name VARCHAR(50)
);

INSERT INTO classes (class_id, class_name, teacher_name) VALUES
(1, 'Math 101', 'Mr. Smith'),
(2, 'English 102', 'Ms. Johnson'),
(3, 'Science 103', 'Dr. Lee');

select * from

update books
set name='Fooled by randomness'